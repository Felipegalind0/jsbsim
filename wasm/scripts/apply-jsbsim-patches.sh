#!/usr/bin/env bash
set -euo pipefail
printf '%s\n' "Vendor patch mutation is retired. Reconcile generic portability in the canonical native fork and update its explicit source lock. The historical patch is preserved in patches/." >&2
exit 1

#!/usr/bin/env node
throw new Error("Separate native source selection is retired. Build the enclosing JSBSim repository with npm --prefix wasm run build. The old source archive and lock are retained historical references.");

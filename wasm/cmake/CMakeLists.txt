cmake_minimum_required(VERSION 3.20)
message(FATAL_ERROR "The detached SDK CMake wrapper is retired. Use npm --prefix wasm run build from the JSBSim repository root; it configures the enclosing root with BUILD_WASM_MODULE=ON.")
